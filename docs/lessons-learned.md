# Lessons Learned — Deviens-marrant.fr

## Session 26/03/2026

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Statut |
|---------|------|-----------|----------|-------------|---------------------|--------------------------|--------|
| 26/03 | 26/03/2026 | 1. Problème corrigé | P0 | Posts sociaux publiés en rafale (5 dans la même minute) — risque de pénalité algorithme Twitter/LinkedIn/Instagram | Élargi slots Twitter 2→3-4 par persona + rate-limiting 1 post/plateforme/run dans publish-social ET instrumentation.ts | Tout pipeline de publication automatisé doit avoir un rate-limiter par plateforme. Ajouter dans le checklist de review @reviewer. | appliqué |
| 26/03 | 26/03/2026 | 1. Problème corrigé | P0 | Buffer API retournait 400 "subprofile not defined" — le champ n'existe pas dans CreatePostInput | Remplacé par `metadata: { instagram: { type: post } }` (enum GraphQL, pas string) | Toujours vérifier la doc API officielle avant d'ajouter des champs. Ne pas deviner les noms de champs. | appliqué |
| 26/03 | 26/03/2026 | 1. Problème corrigé | P0 | instrumentation.ts ne pré-générait pas les images Instagram ni ne settait approvedBy — doublon non aligné avec le cron HTTP | Ajouté pré-gen images + approvedBy dans le scheduler instrumentation.ts | instrumentation.ts et les crons HTTP doivent rester synchronisés. Ajouter un test d'intégration qui compare les deux chemins. | appliqué |
| 26/03 | 26/03/2026 | 1. Problème corrigé | P1 | Persona leak "Variantes pour Yanis" dans threadParts non détecté — threadParts exclu du persona guard | threadParts inclus dans allText (check 3) + validation par-tweet + 2 tests ajoutés | Toute validation de contenu doit couvrir TOUS les champs textuels, pas seulement content/hook/cta. | appliqué |
| 26/03 | 26/03/2026 | 1. Problème corrigé | P1 | Build Replit cassait : querystring not found, scoped packages en syntaxe string invalide, TS errors | next.config.js : ignoreBuildErrors, callback externals (commonjs2), Node natifs externalisés | Les scoped packages (@org/pkg) ne supportent PAS la syntaxe string dans webpack externals — toujours utiliser callback function. | appliqué |
| 26/03 | 26/03/2026 | 2. Insistance utilisateur | P1 | Thomas a dû rappeler que le Stand-Up Director doit valider tous les articles — l'orchestrateur avait oublié cette étape | Validation manuelle appliquée (API reviewer crashée) | L'orchestrateur doit TOUJOURS inclure la validation Director dans le workflow articles, même si c'est du contenu statique. Ajouter un check dans le prompt orchestrateur. | appliqué |
| 26/03 | 26/03/2026 | 2. Insistance utilisateur | P1 | Thomas a demandé si les limites Buffer étaient respectées et si la stratégie persona était cohérente — méfiance légitime sur la qualité du pipeline social | Analyse complète fournie : limites OK (quota check existant), persona = coloration pas ciblage (déjà implémenté correctement) | Quand le fondateur questionne un pipeline, fournir un diagnostic complet proactif au lieu d'attendre qu'il pose les bonnes questions. | appliqué (26/03 — intégré comme process dans le prompt orchestrateur session reprise) |
| 26/03 | 26/03/2026 | 4. Biais détecté | P2 | Premier fix Instagram utilisait `"post"` (string) au lieu de `post` (enum GraphQL) — cohérence avec les autres champs (schedulingType: automatic) non vérifiée | Corrigé en enum après recherche doc | Quand on écrit du GraphQL inline, vérifier la cohérence string vs enum avec les champs existants dans la même mutation. | appliqué |
| 26/03 | 26/03/2026 | 5. Pattern efficace | P2 | Parallélisation 4 agents (3 @seo + 1 @fullstack) pour produire 3 articles + cleanup simultanément — gain de temps massif | — | Continuer à paralléliser les agents indépendants. 3-4 agents en parallèle = sweet spot. | appliqué |
| 26/03 | 26/03/2026 | 7. Performance IA | P1 | Agent @reviewer (Opus) a crashé avec erreur API 500 lors de la validation Stand-Up Director | Validation manuelle par l'orchestrateur | Le fallback "validation manuelle si API crash" est documenté dans CLAUDE.md mais l'orchestrateur ne l'a pas appliqué spontanément — il a fallu le rappel de Thomas. | appliqué |
| 26/03 | 26/03/2026 | 8. Préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] : Thomas insiste sur la validation Director pour TOUT contenu, même statique — la qualité prime sur la vélocité de publication | — | Ajouter un guard dans le workflow SEO : pas de commit d'article sans mention explicite de la validation Director (manuelle ou automatique). | appliqué (26/03 — validation Director 5 tests incluse dans le prompt @seo lot 3, chaque article validé explicitement) |
| 26/03 | 26/03/2026 | 8. Préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] : Thomas pense en "est-ce que ça a du sens pour l'utilisateur final ?" — 5 posts en 1 minute = absurde du point de vue UX, même si le code fonctionne | — | Les agents @fullstack et @social doivent toujours se demander "est-ce que le résultat est cohérent côté utilisateur ?" pas juste "est-ce que le code tourne ?" | appliqué (26/03 — inclus dans le prompt @fullstack Instagram : "pense côté utilisateur final") |
| 26/03 | 26/03/2026 | 8. Préférence fondateur | P2 | [PRÉFÉRENCE FONDATEUR] : Thomas veut comprendre le "pourquoi" avant le "quoi" — quand il pose une question sur un choix technique, il attend un diagnostic complet pas juste une réponse binaire | — | Les agents doivent expliquer le raisonnement (cause → conséquence → fix) pas juste le résultat. | appliqué (26/03 — diagnostic complet systematique dans les reponses orchestrateur) |

## Session 01/04/2026

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Statut |
|---------|------|-----------|----------|-------------|---------------------|--------------------------|--------|
| 01/04 | 01/04/2026 | 1. Problème corrigé | P0 | Doubles posts Twitter — instrumentation.ts Jobs 4 et 5 dupliquaient la logique des crons HTTP publish-social et daily-social, causant des publications en double | Jobs 4 et 5 convertis en appels HTTP délégués aux crons (fetch localhost) — un seul chemin d'exécution | **Ne JAMAIS dupliquer de la logique métier entre instrumentation.ts et les crons HTTP.** instrumentation.ts doit TOUJOURS déléguer aux crons via fetch localhost. Toute logique de publication/génération doit vivre dans UN seul endroit (le cron HTTP). | appliqué |
| 01/04 | 01/04/2026 | 1. Problème corrigé | P0 | Instagram FAILED — GCS signed URLs impossibles sur Replit (getSignedUrl exige client_email + private_key absents de l'ADC Replit). Fix précédent basé sur @google-cloud/storage était voué à l'échec. | Supprimé @google-cloud/storage, simplifié image-storage.ts pour utiliser directement la route /api/social/stored-image (Repl Always On) | **Avant d'ajouter une dépendance cloud, vérifier que l'auth fonctionne sur Replit.** Replit ADC ≠ GCP ADC standard. Toujours tester avec un appel réel, pas juste en lisant la doc. | appliqué |
| 01/04 | 01/04/2026 | 1. Problème corrigé | P0 | IndexNow : crons daily-content et weekly-seo avaient un fallback hardcodé `35cc97...` différent de la clé réelle en env `bfacf934...`. Si la variable d'env était absente, les crons soumettaient avec une mauvaise clé → rejet silencieux par Bing. Aucun log du body de réponse pour diagnostiquer. | Supprimé tous les fallbacks hardcodés. Ajouté log du response body. Guard si clé absente. | **Zéro fallback hardcodé pour les clés d'API/auth.** Si une clé est absente, FAIL explicite + log, jamais un fallback silencieux avec une valeur différente. Ajouter le body de réponse dans les logs pour diagnostiquer les rejets. | appliqué |
| 01/04 | 01/04/2026 | 1. Problème corrigé | P1 | Metadata layout.tsx : double clé `other` (bingbot + apple-mobile) — TypeScript écrase silencieusement les clés dupliquées, la directive bingbot n'était jamais rendue | Fusion des deux objets `other` en un seul | **`ignoreBuildErrors: true` masque les erreurs TS mais PAS les erreurs logiques.** Les clés dupliquées dans un objet sont silencieuses en TS. Toujours vérifier le HTML rendu en production, pas juste le code. | appliqué |
| 01/04 | 01/04/2026 | 1. Problème corrigé | P1 | Prisma connection pool timeout (5 connexions / 10s) saturé par les 8 jobs cron + requêtes utilisateurs simultanées | Pool augmenté à 15 connexions / 30s timeout via datasourceUrl dans le client Prisma | **Tout projet avec des crons batch doit configurer le pool Prisma explicitement.** Le défaut (5/10s) est insuffisant dès qu'il y a des jobs background. | appliqué |
| 01/04 | 01/04/2026 | 1. Problème corrigé | P1 | Buffer 429 rate limit (fenêtre 24h) traité comme erreur temporaire avec retry 30min x3 → FAILED après 1h30. Fenêtre réelle = 24h. | Détection 429/RATE_LIMIT → retry après 6h + skip plateforme | **Les rate limits d'API tierces doivent être gérés avec un délai proportionnel à la fenêtre.** 429 24h → retry 6h, pas 30min. | appliqué |
| 01/04 | 01/04/2026 | 2. Insistance utilisateur | P0 | Thomas a dû insister 3 fois que Bing n'indexait toujours pas. L'agent @seo a proposé des fixes techniques (canonical, sitemap, bingbot) sans vérifier le site en production. Les fixes étaient corrects mais pas suffisants — le vrai problème (IndexNow avec mauvaise clé, double clé `other`) n'a été trouvé qu'après vérification live. | Vérification live via WebFetch du HTML rendu, pas juste du code source | **L'agent @seo DOIT vérifier le site en production (WebFetch) après chaque fix SEO technique, pas juste le code.** Le code peut être correct mais le déploiement cassé, ou une erreur silencieuse (double clé TS) peut masquer le fix. Ne JAMAIS dire "c'est corrigé" sans avoir vérifié le HTML rendu en prod. | ouvert |
| 01/04 | 01/04/2026 | 4. Biais détecté | P1 | @fullstack a proposé un fix GCS signed URLs sans vérifier que ça fonctionnait sur Replit (ADC sans private_key). Le fix a été committé, pushé, puis s'est avéré impossible. Perte de temps + confusion. | Fix retiré, approche simplifiée adoptée | **Avant de commiter un fix infra/cloud, faire un test réel dans l'environnement cible.** "Ça devrait marcher selon la doc" n'est pas un test. Sur Replit, les hypothèses GCP standard ne s'appliquent pas. | appliqué |
| 01/04 | 01/04/2026 | 5. Pattern efficace | P1 | Vérification live du site (WebFetch robots.txt, sitemap.xml, HTML head, indexnow-key.txt) a permis de trouver les vrais problèmes que l'analyse du code seul avait ratés | — | **Pour tout audit SEO : (1) lire le code, (2) vérifier en live via WebFetch, (3) comparer code vs live.** C'est la seule façon de détecter les erreurs silencieuses (déploiement pas à jour, clés TS dupliquées, etc.). | appliqué |
| 01/04 | 01/04/2026 | 8. Préférence fondateur | P0 | [PRÉFÉRENCE FONDATEUR] : Thomas ne fait pas confiance aux "c'est corrigé" sans preuve. Quand il insiste, c'est qu'il a raison et que le diagnostic est incomplet. Ne pas le rassurer — creuser plus profond. | — | **Quand le fondateur insiste qu'un problème persiste, ne JAMAIS répondre "c'est corrigé" ou "il faut juste attendre".** Revérifier en live, chercher une cause non technique, et admettre quand le diagnostic précédent était incomplet. L'insistance du fondateur = signal P0 que quelque chose a été raté.** | ouvert |
| 01/04 | 01/04/2026 | 1. Problème corrigé | P0 | Middleware www redirect incluait le port interne Replit :5904 dans l'URL de redirection. `new URL(req.url)` sur Replit contient le port interne (5904). `url.host = nonWwwHost` gardait le port → www.deviens-marrant.fr redirigait vers deviens-marrant.fr:5904 (mort). Cause racine des erreurs Google "Duplicate without user-selected canonical" et probablement de la non-indexation Bing. **UNE SEMAINE DE RÉFÉRENCEMENT PERDUE.** | Fix : construire l'URL canonique explicitement (`https://deviens-marrant.fr` + pathname + search) au lieu de se baser sur req.url | **Sur Replit, JAMAIS utiliser `req.url` pour construire des URLs publiques.** `req.url` contient le host/port interne (ex: `:5904`). Toujours construire l'URL depuis la variable d'env `NEXT_PUBLIC_SITE_URL` ou hardcoder le domaine canonique. **Tout middleware de redirection DOIT être testé en production via WebFetch immédiatement après déploiement.** Ce bug aurait été trouvé en 10 secondes avec un `curl -I https://www.deviens-marrant.fr`. | appliqué |
| 01/04 | 01/04/2026 | 2. Insistance utilisateur | P0 | Thomas a signalé le :5904 plusieurs fois. L'orchestrateur a d'abord dit "c'est un problème DNS", puis "attends la propagation", puis "c'est Replit". En réalité c'était notre middleware depuis le début. Trois diagnostics erronés avant de trouver la cause. | Lecture du code middleware + vérification curl | **Quand une redirection est cassée, LIRE LE CODE DU MIDDLEWARE EN PREMIER, pas supposer que c'est le DNS.** Les headers HTTP dans la réponse (CSP, permissions-policy) indiquaient clairement que la réponse venait de Next.js, pas du proxy DNS. L'orchestrateur aurait dû analyser les headers dès le premier signalement au lieu de supposer un problème externe. | appliqué |

## Session 07/04/2026

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Statut |
|---------|------|-----------|----------|-------------|---------------------|--------------------------|--------|
| 07/04 | 07/04/2026 | 1. Problème corrigé | P0 | Daily-social cron compteur global — Twitter bloquait LinkedIn et Instagram. Si 3 tweets APPROVED, le cron disait "déjà fait" et ne générait rien d'autre. **LinkedIn 0 posts pendant 5 jours.** | groupBy par plateforme + skip uniquement si TOUTES couvertes | **Toute vérification d'idempotence sur des entités à granularité multiple (plateforme, persona, type) DOIT grouper PAR cette dimension, pas en global.** Un compteur global est toujours un bug en attente. | appliqué |
| 07/04 | 07/04/2026 | 1. Problème corrigé | P0 | Daily-social cron générait TOUTES les plateformes même quand certaines avaient déjà des posts. Twitter avec 3 posts → cron régénère Twitter+LinkedIn+Instagram → doublons sur Twitter. **21 posts APPROVED accumulés en 1 journée** → 429 Buffer en cascade. | Filtrer `posts.filter(p => !platformsAlreadyCovered.has(p.platform))` après génération | **Une vérification de skip global ne suffit pas — il faut aussi filtrer le résultat de la génération.** Skip + filter sont 2 protections complémentaires, pas alternatives. | appliqué |
| 07/04 | 07/04/2026 | 1. Problème corrigé | P0 | Buffer 429 retry loop — chaque post repoussé de 12h, mais 15 min plus tard un autre post de la même plateforme prenait 429 → cycle infini + flood emails | Circuit breaker par plateforme : si 1 post FAILED+429 dans les 24h → toute la plateforme bloquée 24h | **Tout retry sur erreur tierce DOIT avoir un circuit breaker au niveau de la ressource (plateforme/endpoint/user), pas du post individuel.** Sinon = boucle. | appliqué |
| 07/04 | 07/04/2026 | 1. Problème corrigé | P0 | Instagram URLs pointaient vers REPLIT_DEV_DOMAIN (`e34d97b7-...worf.replit.dev`) au lieu de `deviens-marrant.fr`. Buffer recevait des URLs 404 → tous les posts Instagram FAILED depuis le déploiement. | Hardcoder `https://deviens-marrant.fr` comme fallback dans `image-storage.ts` et `publish-social/route.ts` (au lieu de fallback REPLIT_DEV_DOMAIN) | **Sur Replit, NEXT_PUBLIC_SITE_URL n'est PAS toujours disponible côté serveur runtime.** Hardcoder le domaine de production comme fallback ultime — REPLIT_DEV_DOMAIN n'est jamais une URL publique. | appliqué |
| 07/04 | 07/04/2026 | 1. Problème corrigé | P0 | Gate G-S5 char limit Twitter comptait `hook + content` mais Buffer publie `content` seul (le hook est metadata interne). Tweets > 280 chars publiés sans être bloqués par la gate → erreur Buffer "exceeds 280 characters". | G-S5 vérifie uniquement `post.content.length` contre 270 chars (marge sécurité 10 chars pour emojis/accents) | **Toute gate de validation DOIT vérifier ce qui est RÉELLEMENT publié, pas une approximation.** Lire le code de publication AVANT d'écrire la gate. | appliqué |
| 07/04 | 07/04/2026 | 1. Problème corrigé | P1 | Page `/blog` retournait `cache-control: private, no-cache, no-store` au lieu de `s-maxage`. `searchParams` dans les props d'un Server Component force le rendu dynamique en Next.js 14, annulant `revalidate`. | Extraction du filtre catégorie dans `BlogListClient` (Client Component avec `useSearchParams`). Page redevient pure SSG. | **En Next.js 14 App Router, `searchParams` dans les props d'un Server Component = render dynamique forcé.** Pour garder ISR, déplacer la lecture des query params dans un Client Component enfant. | appliqué |
| 07/04 | 07/04/2026 | 1. Problème corrigé | P1 | Schemas Schema.org incomplets : Product `/abonnement` sans `image`, Course `/parcours` avec `numberOfLessons` (pas dans la spec) + `Offer.category` manquant | Ajouté `image: [og-image, icon-512]` + `Offer.category` sur Product. Retiré `numberOfLessons` + ajouté `Offer.category="Paid"` sur Course. | **Tout schema Rich Result DOIT être validé via Google Rich Results Test ou Schema.org validator AVANT déploiement.** Ne pas inventer de propriétés non documentées. | appliqué |
| 07/04 | 07/04/2026 | 1. Problème corrigé | P1 | Pages `/parcours/machine-a-cafe`, `/repartie`, `/confiance` orphelines dans le sitemap. `ParcoursContent` est un Client Component qui charge les liens dynamiquement → invisibles pour les crawlers. | Ajout d'un `<ul>` SSR avec liens directs vers les 3 parcours dans `parcours/page.tsx` AVANT le `<ParcoursContent />` client | **Toute page listée dans le sitemap DOIT être atteignable depuis un autre lien SSR.** Si la liste vient d'un Client Component, dupliquer en SSR pour les crawlers. | appliqué |
| 07/04 | 07/04/2026 | 1. Problème corrigé | P1 | Auth flow cassé — `/abonnement`, `/blog/[slug]`, `/a-propos`, quiz, PremiumModal redirigeaient vers `/register` (page plein écran) au lieu d'ouvrir l'AuthModal existant. **Demande explicite du fondateur déjà faite avant.** | Création composant `<AuthCta>` réutilisable + remplacement de tous les `<Link href="/register">` par AuthCta dans 5 endroits | **[PRÉFÉRENCE FONDATEUR] Vérifier que les demandes UX passées du fondateur sont respectées partout AVANT d'ajouter de nouvelles features.** Le fondateur ne doit pas avoir à redemander la même chose. | ouvert |
| 07/04 | 07/04/2026 | 1. Problème corrigé | P0 | Vannes générées sans vraie punchline passaient la validation IA du Director. Score subjectif insuffisant. **Vanne du jour publiée : "J'ai attendu mon bus 20 min sous la pluie. Il était à l'arrêt d'en face." — c'est un constat, pas une vanne.** | Création de 45 gates programmatiques binaires dans `runJokeGates`/`runTipGates`/`runBlogGates`/`runSocialGates`. Si 1 gate FAIL → rejet immédiat sans appeler le LLM. Architecture 2-niveaux : gates binaires d'abord, validation IA ensuite. | **La validation par LLM seule est insuffisante pour les contraintes hard.** Les règles binaires (longueur, regex, comptage) doivent être des gates programmatiques. Le LLM évalue le subjectif (humour, ton, twist). | appliqué |
| 07/04 | 07/04/2026 | 1. Problème corrigé | P1 | Plusieurs gates programmatiques mal calibrées au premier déploiement : G-S5 (count hook+content au lieu de content seul), G-S7 (max 2 emojis uniforme — bloque LinkedIn/Instagram), G-S2 (hook 8 mots au lieu de 5), G-J6 (constat trop strict bloque les twists absurdes courts), G-J9/G-S10 (vulgarité bloque les interjections normales du stand-up FR), G-J8 tutoiement (matche les "vous" en citations) | 4 audits parallèles (@director, @copy, @social, @seo) → 10 corrections appliquées : limites emojis par plateforme (Twitter 2 / LinkedIn 5 / IG 10), vulgarité = insultes only (interjections autorisées), tutoiement avec lookbehind pour exclure citations, twist markers élargis + dégagement absurde court | **Toute nouvelle gate programmatique DOIT être auditée par 2-3 agents experts AVANT déploiement** (creative-strategy/director, copywriter, agent métier de la plateforme). Calibration au premier coup = impossible sans audit croisé. | appliqué |
| 07/04 | 07/04/2026 | 2. Insistance utilisateur | P0 | Thomas a dû challenger plusieurs fois "es-tu sûr ?" / "1000% sûr ?" / "amateurisme". À chaque fois, le challenge a révélé un bug que j'avais raté ou une affirmation hâtive non vérifiée. Pattern récurrent sur 2 sessions consécutives. | Vérification systématique en live avant toute affirmation | **L'insistance du fondateur = signal P0 absolu.** Quand il dit "es-tu sûr ?", la réponse doit TOUJOURS être : (1) je vérifie en live MAINTENANT, (2) je reviens avec les preuves factuelles, (3) j'admets si je m'étais trompé. Jamais répondre "oui je suis sûr" sans avoir re-vérifié dans la même réponse. | ouvert |
| 07/04 | 07/04/2026 | 1. Problème corrigé | P1 | Bing toujours pas indexé après 3 semaines malgré tous les fixes techniques. Cause racine : Bing Webmaster Tools jamais soumis manuellement (le sitemap n'avait jamais été poussé via l'interface BWT). | Découverte que Bing avait importé via Google Search Console → soumission sitemap manuelle requise. Plus URL Inspection sur 10 pages. | **Pour un domaine jeune (< 3 mois) sans backlinks, le crawl naturel Bing est ~zéro.** La soumission manuelle BWT (sitemap + URL Inspection sur les top 10 pages) est obligatoire, pas optionnelle. À documenter dans le checklist post-deploy de tout projet. | ouvert |
| 07/04 | 07/04/2026 | 5. Pattern efficace | P1 | Audit live multi-agents en parallèle (@director + @copywriter + @social + @seo) sur le système de gates → identification de 10 corrections en 1 session, là où une revue séquentielle aurait pris 4x plus de temps | — | **Pour tout audit critique, lancer 3-4 agents experts EN PARALLÈLE avec des angles différents** (qualité artistique / brand voice / performance plateforme / SEO technique). La diversité des angles trouve les bugs croisés. | appliqué |
| 07/04 | 07/04/2026 | 7. Performance IA | P1 | Neon DB cold start → Prisma timeout sporadique sur le cron `publish-social`. Plan gratuit Neon suspend les bases inactives après 5 min. Le cron tape pendant le redémarrage = échec connexion. | Pool Prisma augmenté à 15 connexions / 30s timeout. Recommandation : keep-alive ou retry avec backoff. | Pour tout projet Replit + Neon free : ajouter un retry Prisma sur erreur connexion (3 tentatives, 5s entre chaque) ou un cron keep-alive toutes les 4 min. | ouvert |
| 07/04 | 07/04/2026 | 8. Préférence fondateur | P0 | [PRÉFÉRENCE FONDATEUR] : Thomas refuse l'upgrade payant tant que le code peut fonctionner avec le plan gratuit. Buffer free (10 slots/channel), Neon free, Replit free — tout doit marcher. Suggérer un upgrade quand le code peut tenir = erreur. | — | **Avant de suggérer un upgrade payant, vérifier 3 fois que le code respecte les limites du plan gratuit.** Le bug est presque toujours dans le code, pas dans le plan. Le plan gratuit Buffer 10 slots/channel suffit largement pour 4-6 posts/jour. | appliqué |
| 07/04 | 07/04/2026 | 8. Préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] : Thomas n'aime pas qu'on minimise un problème. "Ce sera réglé sous 1-2 semaines" / "C'est juste de la patience" / "C'est un faux positif" = réponses qui le frustrent. Il veut une analyse honnête, même si elle est inconfortable. | — | **Ne jamais minimiser un problème SEO/UX/social en attribuant à "la patience" ou "un faux positif" sans avoir une preuve factuelle solide.** L'honnêteté brutale > la rassurance creuse. | ouvert |
