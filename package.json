{
  "name": "deviens-marrant-monorepo",
  "private": true,
  "scripts": {
    "postinstall": "cd apps/web && npm install && npx prisma generate",
    "build": "cd apps/web && npx prisma migrate deploy && bash scripts/seed.sh && npm run build && cp -r public .next/standalone/public && cp -r .next/static .next/standalone/.next/static",
    "start": "cd apps/web && HOSTNAME=0.0.0.0 node .next/standalone/server.js"
  },
  "devDependencies": {
    "@swc/core": "^1.15.21",
    "@swc/jest": "^0.2.39",
    "jest-environment-jsdom": "^30.3.0",
    "prisma": "6.19.2",
    "sharp": "^0.34.5"
  },
  "dependencies": {
    "@prisma/client": "6.19.2",
    "@replit/object-storage": "^1.0.0"
  }
}
